---
// Component Imports
import Layout from '../../components/Layout.astro';

// Data Imports
import quotes from '../../../public/all_quotes.json';

// Constants
const base = import.meta.env.BASE_URL;

// Random Quote Logic
const allBooks = Object.values(quotes);
const randomBookIndex = Math.floor(Math.random() * allBooks.length);
const randomBook = allBooks[randomBookIndex];
const randomQuoteIndex = Math.floor(Math.random() * randomBook.quotes.length);
const randomQuote = randomBook.quotes[randomQuoteIndex];
---

<Layout title="Underlines" description="A collection of quotes from the books I've read.">
  <div class="project-page">
    <!-- Page Header -->
    <header class="page-header">
      <h1 class="page-title">Underlines</h1>
      <p class="page-description">A collection of quotes from the books I've read.</p>
      <hr class="page-rule" />
    </header>

    <!-- Random Quote Block -->
    <div class="random-quote-block">
      <h3 class="random-quote-title">One random quote:</h3>
      <blockquote class="random-quote">
        <p>{randomQuote}</p>
        <footer>
          <cite>{randomBook.title} - {randomBook.author}</cite>
        </footer>
      </blockquote>
    </div>

    <!-- Quotes List -->
    <div class="quotes-list">
      {Object.values(quotes).map(book => (
        <details class="book-entry">
          <summary class="book-title">
            {book.title} - <span class="book-author">{book.author}</span>
          </summary>
          <div class="quotes-content">
            <ul>
              {book.quotes.map(quote => (
                <li>{quote}</li>
              ))}
            </ul>
          </div>
        </details>
      ))}
    </div>

    <!-- Page Navigation -->
    <div class="page-navigation">
      <a href={`${base}projects`} class="back-link">← Back to All Projects</a>
    </div>
  </div>
</Layout>

<style>
  /* General Page Styles */
  .project-page {
    max-width: 800px;
    margin: 0 auto;
  }

  .page-header {
    text-align: center;
    margin-bottom: calc(var(--grid-unit) * 8);
  }

  .page-title {
    font-family: var(--font-heading-primary);
    font-size: 3.5rem;
    font-weight: 900;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    line-height: 1.1;
    margin-bottom: calc(var(--grid-unit) * 3);
  }

  .page-description {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin-bottom: calc(var(--grid-unit) * 4);
  }

  .page-rule {
    height: 4px;
    background-color: var(--color-accent);
    border: none;
    width: 120px;
    margin: 0 auto;
  }

  /* Random Quote Styles */
  .random-quote-block {
    margin-bottom: calc(var(--grid-unit) * 8);
    text-align: center;
    padding: calc(var(--grid-unit) * 4);
    background-color: var(--color-surface);
    border-radius: 4px;
    border: 1px solid var(--color-border);
  }

  .random-quote-title {
    font-family: var(--font-heading-secondary);
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--color-text-secondary);
    margin-bottom: calc(var(--grid-unit) * 2);
  }

  .random-quote {
    font-size: 1.5rem;
    font-style: italic;
    color: var(--color-text-primary);
    margin: 0;
  }

  .random-quote footer {
    margin-top: calc(var(--grid-unit) * 2);
    font-size: 1rem;
    font-style: normal;
    color: var(--color-text-muted);
  }

  /* Quotes List Styles */
  .quotes-list {
    display: flex;
    flex-direction: column;
    gap: calc(var(--grid-unit) * 2);
    margin-bottom: calc(var(--grid-unit) * 8);
  }

  .book-entry {
    border: 1px solid var(--color-border);
    border-radius: 4px;
    overflow: hidden;
  }

  .book-title {
    font-family: var(--font-heading-secondary);
    font-size: 1.2rem;
    font-weight: 700;
    padding: calc(var(--grid-unit) * 2);
    cursor: pointer;
    background-color: var(--color-surface);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .book-author {
    font-family: var(--font-body);
    font-size: 0.9rem;
    color: var(--color-text-muted);
    font-weight: 400;
  }

  .book-title:hover {
    background-color: var(--color-surface-hover);
  }

  .quotes-content {
    padding: calc(var(--grid-unit) * 2);
    background-color: var(--color-background);
  }

  .quotes-content ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
  }

  .quotes-content li {
    padding: calc(var(--grid-unit) * 1.5) 0;
    border-bottom: 1px solid var(--color-border-light);
    line-height: 1.6;
  }

  .quotes-content li:last-child {
    border-bottom: none;
  }

  /* Page Navigation Styles */
  .page-navigation {
    text-align: center;
    padding-top: calc(var(--grid-unit) * 4);
    border-top: 1px solid var(--color-border);
  }

  .back-link {
    font-family: var(--font-heading-secondary);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-accent);
    padding: calc(var(--grid-unit)) calc(var(--grid-unit) * 2);
    border: 2px solid var(--color-accent);
    border-radius: 4px;
    transition: all 0.2s ease;
    display: inline-block;
  }

  .back-link:hover,
  .back-link:focus {
    background-color: var(--color-accent);
    color: white;
    text-decoration: none;
  }
</style>